import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { LogginPage } from './loggin';

@NgModule({
  declarations: [
    LogginPage,
  ],
  imports: [
    IonicPageModule.forChild(LogginPage),
  ],
})
export class LogginPageModule {}
